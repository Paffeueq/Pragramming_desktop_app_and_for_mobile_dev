# Blazor Guidelines

- Use camelCase for component parameters.
- Use PascalCase for component class names.
- Keep UI logic in `.razor.cs` files and minimal logic in `.razor`.
- Use proper naming for event handlers: `OnXyz`.
